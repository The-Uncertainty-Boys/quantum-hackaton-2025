import networkx as nx
from ortools.sat.python import cp_model

# def schedule(trap: nx.Graph, sequence: list):
#     print("schedule")

def schedule(trap: nx.Graph, sequence: list) -> tuple:
    positions_history = [0]
    gates_schedule = [0]



    return positions_history, gates_schedule
